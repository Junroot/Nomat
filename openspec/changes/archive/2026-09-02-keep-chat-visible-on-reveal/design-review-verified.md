# 검증 사실 캐시

이 루프 실행 중 실제 코드를 열어 확인한 관찰 사실. 코드는 루프 중 불변이므로
같은 루프의 후속 에이전트는 이 관찰을 직접 확인한 것과 동등하게 신뢰해도 된다.
사실만 담는다 — 심각도·지적·권고·평가 금지.

- `front/app/routes/RoomView.tsx` 전체 284줄 (라운드 1)
- `RoomView.tsx:69-71` — `showGate = isPlaying && !armed && !isDeactivated`, `showReveal = round.phase === "REVEAL" && !showGate`, `showResult = round.phase === "ENDED" && !resultClosed` (라운드 1)
- `RoomView.tsx:91-95` — `useEffect(() => { if (isNearBottomRef.current) messagesEndRef.current?.scrollIntoView({ behavior: "smooth" }) }, [messages])`. 의존성은 `messages` 하나 (라운드 1)
- `RoomView.tsx:115-123` — `if (isLoading || !roomDetail)` 조기 반환. 이 분기는 `ColumnsContainer`·`Column2`·`RoundAudioPlayer`를 모두 렌더하지 않는 별도 트리다 (라운드 1)
- `RoomView.tsx:132-133,169` — `ColumnsContainer`의 JSX 자식은 `{(!isMobile || showInfo) && <Column1>…}`와 `<Column2>` 두 표현식. `Column2`에는 조건이 붙어 있지 않다 (라운드 1)
- `RoomView.tsx:186-188` — `{isPlaying && <RoundPanel round={round} playback={playback} />}` (라운드 1)
- `RoomView.tsx:189-197` — 메시지 컨테이너 div, `className="px-4 pt-4 w-full h-full shrink-1 flex flex-col gap-0.5 overflow-auto"`, `onScroll`에서 `isNearBottomRef.current = el.scrollHeight - el.scrollTop - el.clientHeight < 80` (라운드 1)
- `RoomView.tsx:257-264` — `<RoundAudioPlayer …/>`는 `</ColumnsContainer>` 뒤, `AppShell` 직속 자식으로 렌더된다 (라운드 1)
- `RoomView.tsx:265-266` — `{showGate && <AudioGateOverlay …/>}`, `{showReveal && <RoundRevealOverlay round={round} />}`. DOM 순서상 `RoundAudioPlayer` 뒤 (라운드 1)
- `RoomView.tsx:270-281` — `isDeactivated` 오버레이는 `fixed inset-0 z-50 … bg-zinc-950/80 backdrop-blur-sm` (라운드 1)
- `front/app/components/ui/ClipPlayer.tsx:79-81` — visible 분기 클래스 `"fixed left-1/2 top-[8vh] -translate-x-1/2 z-50 w-[min(92vw,720px,80vh)] aspect-video overflow-hidden rounded-2xl bg-black ring-1 ring-neon-cyan/30 shadow-2xl pointer-events-none [&_iframe]:absolute …"` (라운드 1)
- `ClipPlayer.tsx:82-83` — 비노출 분기는 `"hidden"` 하나. 주석에 "`hidden`(display:none)은 iframe을 재로드하지 않는다 — 소리는 그대로 난다" (라운드 1)
- `ClipPlayer.tsx:61-78` — visible 분기 앞 주석 블록에 iframe 재부모화 금지, `RoundRevealOverlay` 상단 여백과 같은 식으로 맞물림, 유튜브 제목·로고를 가리지 않는 판단, `pointer-events-none` 사유가 기술돼 있다 (라운드 1)
- `ClipPlayer.tsx:17,40-55,87-88` — `EMPTY_VIDEO_ID = ""`, `options = useMemo(..., [])`, `<YouTube videoId={EMPTY_VIDEO_ID} opts={options} …>` (라운드 1)
- `front/app/components/ui/RoundRevealOverlay.tsx:28` — 루트 div 클래스 `"fixed inset-0 z-40 flex flex-col items-center justify-start gap-4 pt-[calc(8vh_+_min(92vw,720px,80vh)_*_0.5625_+_1.5rem)] bg-zinc-950/85 backdrop-blur-sm px-6 text-center"` (라운드 1)
- `RoundRevealOverlay.tsx:7-21` — 주석에 좌표 복제("상단 여백을 영상과 똑같은 식으로 계산해 맞물린다", "둘 중 하나만 바꾸면 겹치거나 사이가 벌어진다")가 기술돼 있다 (라운드 1)
- `RoundRevealOverlay.tsx:25,30,34-42` — `round.winnerNickname`, `round.roundNumber`/`round.totalRounds`, `round.title`만 읽는다. `round` 외 prop 없음 (라운드 1)
- `front/app/components/ui/RoundAudioPlayer.tsx:38` — `const revealing = params.phase === "REVEAL";` (라운드 1)
- `RoundAudioPlayer.tsx:40-48` — Fragment 안에서 `Array.from({ length: PLAYER_COUNT })`로 `ClipPlayer`를 `key={index}`, `visible={revealing && activeIndex === index}`로 렌더한다. 다른 DOM 래퍼가 없다 (라운드 1)
- `front/app/components/ui/RoundPanel.tsx:7-12` — props는 `{ round: RoundState; playback: ClipPlaybackStatus }` 두 개뿐 (라운드 1)
- `RoundPanel.tsx:31,49-53,56-65,68-78` — `isOpen = round.phase === "OPEN"`. 카운트다운·재생 실패/차단 안내는 `isOpen` 게이트, 점수판(`rows.length > 0`)은 phase 무관 (라운드 1)
- `RoundPanel.tsx:34` — 루트 클래스 `"mx-2 mt-2 p-3 md:p-4 flex flex-col gap-3 bg-zinc-800 rounded-2xl"`. 정답(`title`)·승자(`winnerNickname`) 표시는 현재 없다 (파일 전체 82줄 확인) (라운드 1)
- `front/app/components/layout/Column2.tsx:5` — `className="w-full flex-1 md:flex-2 flex flex-col px-1 gap-4 min-h-0"` (라운드 1)
- `front/app/components/layout/ColumnsContainer.tsx:5` — `className="w-full h-full flex flex-col md:flex-row md:flex-nowrap overflow-auto"` (라운드 1)
- `front/app/components/layout/MobileHeader.tsx:25,44` — 두 variant 모두 헤더 높이 `h-[56px]` (라운드 1)
- `front/app/components/ui/AudioGateOverlay.tsx:14` — `"fixed inset-0 z-50 flex flex-col items-center justify-center gap-6 bg-zinc-950/80 backdrop-blur-sm"` (라운드 1)
- `front/app/components/layout/AppShell.tsx:137-150` — `SubShell`은 `isMobile ? <div …><MobileHeader/><AnimatedContent className="flex-1 overflow-auto">{children}</AnimatedContent></div> : <AnimatedContent className="flex-1 h-full">{children}</AnimatedContent>` 로 children을 서로 다른 엘리먼트 타입 아래에 둔다 (라운드 1)
- `front/app/hooks/roundReducer.ts:16-38` — `RoundState`에 `phase`, `roundSeq`, `roundNumber`, `totalRounds`, `deadlineAt`, `currentTrack`, `nextTrack`, `title`, `winnerId`, `winnerNickname`, `scores` 존재. `title`은 "REVEAL/ENDED에서만 채워짐" 주석 (라운드 1)
- `roundReducer.ts:18-27` — `roundSeq` 주석 "라운드당 2씩 증가(`OPEN→REVEAL`, `REVEAL→다음 OPEN`)… 표기에 쓰면 Round 13 / 9" (라운드 1)
- `roundReducer.ts:64-66,110-112` — `GAME_STARTED`는 `initialRoundState` 반환(title null), `GAME_ENDED`는 `{ ...state, phase: "ENDED", deadlineAt: null }` (라운드 1)
- `front/app/hooks/useRoomSubscription.ts:139-159` — `STARTED` 핸들러가 `setStatus("PLAYING")` + `dispatchRound(GAME_STARTED)`를, `ENDED` 핸들러가 `setStatus("ACTIVE")` + `dispatchRound(GAME_ENDED)`를 같은 함수 안에서 연속 호출한다 (라운드 1)
- `useRoomSubscription.ts:215-225` — `fetchRoomDetail().then()` 안에서 `setStatus(detail.status)`와 `dispatchRound(HYDRATE)`를 연속 호출한다 (라운드 1)
- `front/app/hooks/useRoundAudioOrchestrator.ts:12,75` — `PLAYER_COUNT = 2`, `activeIndex = Math.max(roundNumber - 1, 0) % PLAYER_COUNT` (라운드 1)
- `useRoundAudioOrchestrator.ts:176-189` — REVEAL 재생 이펙트는 `phase !== "REVEAL" || !armed || !track || revealReplayedRef.current === roundNumber` 이면 반환한다 (armed 필요) (라운드 1)
- `useRoundAudioOrchestrator.ts:191-214` — 선버퍼링은 `1 - activeIndex` 플레이어에 `mute()` 후 `loadVideoById`. `phase === "ENDED"` 이면 두 플레이어 모두 `stopVideo()` (라운드 1)
- `useRoundAudioOrchestrator.ts` 전체 308줄 — DOM 참조·요소 크기 측정·`document` 접근이 없다. 플레이어 제어는 전부 `playersRef`의 핸들 메서드 호출이다 (전체 열람) (라운드 1)
- `front/app/app.css:29` — `@utility shadow-glow-cyan` 정의 존재 (`shadow-glow-cyan-lg`는 43행) (라운드 1)
- `back/src/main/kotlin/ilpak/nomat/room/application/RoundService.kt:62-68` — `submitAnswer` KDoc "채팅 원문 방송은 호출자(`RoomStompController`)가 정답 여부와 무관하게 항상 수행", 본문 첫 가드가 `if (snapshot.phase != RoundPhase.OPEN) return` (라운드 1)
- `back/src/main/kotlin/ilpak/nomat/room/out/RoundStateStoreImpl.kt:145` — `private const val REVEAL_MILLIS = 5_000L` (라운드 1)
- `openspec/specs/room-round-ui/spec.md` — `### Requirement:` 헤딩 9개. 그중 "게임 중 채팅 입력이 정답 추측이며 별도 정답 입력 UI가 없다"(23행)와 "ROUND_REVEALED 수신 시 정답·승자·점수판을 표시한다"(35행)가 존재하며, 이 change의 `specs/room-round-ui/spec.md` MODIFIED 헤딩 두 개와 문자열이 일치한다 (라운드 1)
- `openspec/specs/room-round-ui/spec.md:35-78` 대비 change delta — delta는 기존 본문·시나리오를 모두 포함하고, 게이트 관련 불릿·시나리오와 화면 점유 금지·스크롤 유지 문단 및 시나리오 4개를 추가한다. 삭제된 기존 시나리오는 없다 (라운드 1)
