# 검증 사실 캐시

이 루프 실행 중 실제 코드를 열어 확인한 관찰 사실. 코드는 루프 중 불변이므로
같은 루프의 후속 에이전트는 이 관찰을 직접 확인한 것과 동등하게 신뢰해도 된다.
사실만 담는다 — 심각도·지적·권고·평가 금지.

- `front/app/hooks/useRoundAudioOrchestrator.ts:7` — `const PLAYBACK_VOLUME = 50;` (라운드 1)
- `front/app/hooks/useRoundAudioOrchestrator.ts:76-79` — `activeIndexRef`·`phaseRef`는 `useRef(값)` 선언 직후 렌더 중 `.current = 값` 으로 갱신하는 패턴 (라운드 1)
- `front/app/hooks/useRoundAudioOrchestrator.ts:81` — `playersRef = useRef<(YouTubePlayerHandle | null)[]>([null, null])` (라운드 1)
- `front/app/hooks/useRoundAudioOrchestrator.ts` 전체 열람 — `setVolume(PLAYBACK_VOLUME)` 호출은 113(`startActivePlayback`)·146(`loadCurrentTrack` armed 분기)·186(REVEAL 재재생 이펙트)·232(`makeOnReady`) 네 곳. `unMute()` 호출은 112·145·185 세 곳. `mute()` 호출은 199(선버퍼링 이펙트)·240(`makeOnReady` 비담당 분기) 두 곳 (라운드 1)
- `front/app/hooks/useRoundAudioOrchestrator.ts:112-114,145-147,185-188` — 세 unMute 지점 모두 순서가 `unMute()` → `setVolume(PLAYBACK_VOLUME)` → `playVideo()`/`loadVideoById()`/`seekTo()+playVideo()` (라운드 1)
- `front/app/hooks/useRoundAudioOrchestrator.ts:128-134` — `loadCurrentTrack`에서 이미 선버퍼링된 트랙이면 `seekTo` 후 `armed`일 때 `startActivePlayback` 호출(그 안에서 unMute·setVolume·playVideo) (라운드 1)
- `front/app/hooks/useRoundAudioOrchestrator.ts:218-225` — `armed` 변경 이펙트는 담당 플레이어에 `startActivePlayback` 호출 (라운드 1)
- `front/app/hooks/useRoundAudioOrchestrator.ts:256-264` — 비담당 플레이어는 `PLAYING` 상태 관찰 시 `pauseVideo()` (라운드 1)
- `front/app/hooks/useRoundAudioOrchestrator.ts:64-65` — 훅 문서 주석에 "이 훅은 **명령형 제어만** 담당하고 플레이어를 렌더링하지 않는다" 서술 존재 (라운드 1)
- `front/app/hooks/useClipPlayback.ts:100-115` — `handleStateChange`는 state 1(PLAYING)·3(BUFFERING)만 분기. 볼륨 관련 코드 없음(파일 전체 열람) (라운드 1)
- `front/app/components/ui/RoundAudioPlayer.tsx:9-14,30-31` — `RoundAudioPlayerProps extends RoundAudioOrchestratorParams`, `...params`를 그대로 `useRoundAudioOrchestrator(params)`에 전달 (라운드 1)
- `front/app/components/ui/RoundAudioPlayer.tsx:26-28` — StrictMode 이중 마운트로 "볼륨이 컸다가 작아지는" 설명 주석 존재 (라운드 1)
- `front/app/components/ui/ClipPlayer.tsx:40-55,95-105` — `opts`는 `useMemo(..., [])`, `videoId`는 상수 `""`. `onReady`/`onStateChange` 콜백을 매 렌더 새로 넘김 (라운드 1)
- `front/app/components/ui/MusicPlayer.tsx:23` — `playerRef = useRef<any>(null)` (라운드 1)
- `front/app/components/ui/MusicPlayer.tsx:28-31` — `onReady`에서 `playerRef.current.setVolume(50)` (라운드 1)
- `front/app/components/ui/MusicPlayer.tsx:33-47` — `youTubeOptions`가 매 렌더 새 객체로 생성되며 `playerVars`에 `start: startTimeSec, end: endTimeSec` 포함 (라운드 1)
- `front/app/components/ui/MusicPlayer.tsx` 전체 열람 — `mute`/`unMute` 호출 없음. 재생은 사용자 클릭(`togglePlay`)으로만 시작 (라운드 1)
- `front/app/components/layout/NavigationBar.tsx` 전체(11줄) — `className="h-full w-[72px] py-[16px] hidden md:flex flex-col items-center gap-[8px]"`, `children`만 렌더 (라운드 1)
- `front/app/components/layout/AppShell.tsx:82-84,134-136` — `MainShell`·`SubShell` 각각 `<div className="grow-0 shrink-0"><Me /></div>`를 `NavigationBar` 마지막 자식으로 배치 (라운드 1)
- `front/app/components/layout/AppShell.tsx:87-90,139` — 골격 고정("isMobile로 엘리먼트 트리를 갈아끼우지 않는다") 주석 존재. `NavigationBar`는 `children`을 감싸는 `AnimatedContent`와 형제 (라운드 1)
- `front/app 전체 grep "NavigationBar"` — 사용처는 `AppShell.tsx`(import 1, JSX 2)뿐 (라운드 1)
- `front/app 전체 grep "<Me "` — `AppShell.tsx` 2곳, `MobileHeader.tsx:27` `<Me compact />` 1곳 (라운드 1)
- `front/app/routes/LoginView.tsx grep "NavigationBar|AppShell"` — 해당 없음 (라운드 1)
- `front/app/hooks/useBreakpoint.ts:9` — `MOBILE_QUERY = "(max-width: 767px)"` (라운드 1)
- `front/app/components/ui/AudioGateOverlay.tsx` 전체 열람 — props `onArm`만. `hidden md:block`/`md:hidden`으로 갈라 쓴 안내 문구 블록(19-23) 존재. 스토어 참조 없음 (라운드 1)
- `front/app/routes/RoomView.tsx:196` — `{showGate && <AudioGateOverlay onArm={() => setArmed(true)} />}` (라운드 1)
- `front/app/components/ui/ChatInput.tsx:31-40` — `window.addEventListener("keydown", ...)`로 `Enter`이면서 `activeElement !== inputRef.current`일 때 `preventDefault` 후 입력창 포커스. `Escape` 처리 없음 (라운드 1)
- `front/app/components/ui/ChatInput.tsx:59-70` — 입력창 `onKeyDown`에서 `Enter`(IME 조합 제외) 시 `shiftKey`면 `onPass`, 아니면 전송 (라운드 1)
- `front/app/components/ui/Dropdown.tsx:24` — `document.addEventListener("mousedown", handleClickOutside)` 패턴 존재 (라운드 1)
- `front/app 전체 grep "localStorage|sessionStorage"` — 사용처 없음 (라운드 1)
- `front/app/stores/` — `MeStore.ts`·`RoomConnectionStore.ts` 두 파일. 둘 다 `create<State>()((set) => ({...}))` 형태, 미들웨어 없음 (라운드 1)
- `front/app/assets/` 목록 — 볼륨/스피커/mute 이름의 SVG 없음(21개 파일 전수) (라운드 1)
- `front/package.json:23` — `"zustand": "^5.0.3"`, 설치본 `node_modules/zustand/package.json` version `5.0.3`. `react-youtube` 설치본 10.1.0 (라운드 1)
- `node_modules/zustand/esm/middleware.mjs` `createJSONStorage` — `getStorage()` 호출을 try/catch로 감싸 예외 시 `undefined` 반환. 반환된 storage의 `setItem`은 `storage.setItem(name, JSON.stringify(...))`를 try/catch 없이 호출. `getItem`은 `JSON.parse`를 try/catch 없이 호출 (라운드 1)
- `node_modules/zustand/esm/middleware.mjs` `persistImpl` — `options.storage`가 falsy면 `console.warn` 후 메모리 `set`만 하는 config로 동작. 그 외에는 `set` 래퍼가 `set(...args); void setItem();` 순으로 호출 (라운드 1)
- `node_modules/zustand/esm/middleware.mjs` `persistImpl.hydrate` — `toThenable(storage.getItem)` 사용. `toThenable`은 동기 함수의 예외를 잡아 `.catch` 경로로 보내고 `.then` 체인을 건너뜀. 동기 storage면 `.then` 체인이 즉시 실행되어 `options.merge(migratedState, get() ?? configResult)` 결과를 `set(state, true)`로 반영. 스토어 생성 시 `skipHydration`이 아니면 `hydrate()`를 즉시 호출하고 `stateFromStorage || configResult`를 반환 (라운드 1)
- `openspec/specs/room-round-ui/spec.md` 전체 grep "볼륨|50|음소거|mute|setVolume" — 볼륨 값(50)을 규정하는 요구사항 없음. 358행에 "선버퍼링된 트랙은 ... 소리를 내서는 안 된다(SHALL NOT)" 및 "선버퍼링 쪽은 반드시 음소거되어야 한다" 서술. 167행에 정답 공개 구간 정답 곡 재재생 요구 (라운드 1)
- `openspec/specs/` — `playback-volume` 디렉터리 없음(신규 capability) (라운드 1)
- YouTube IFrame API 공식 레퍼런스(developers.google.com/youtube/iframe_api_reference) — `setVolume`: "Sets the volume. Accepts an integer between 0 and 100." `getVolume`: "will return the volume even if the player is muted." `setVolume`이 mute 상태를 바꾸는지, 볼륨 0이 mute와 같은지에 대한 서술 없음 (라운드 1)
