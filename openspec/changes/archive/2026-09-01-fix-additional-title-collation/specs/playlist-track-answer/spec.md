## ADDED Requirements

### Requirement: 추가 정답의 유일성은 문자 단위 일치로 판정한다

시스템은 한 트랙의 추가 정답 목록에서 두 값이 같은지를 **코드포인트 단위 일치**로 판정해야(SHALL) 한다. 저장소(MySQL)의 문자열 비교 규칙이 표기가 다른 값을 동일한 값으로 취급해 저장을 거부해서는 안 된다(MUST NOT).

이것은 **저장 계층의 보증**이다. 무엇을 같은 정답으로 볼지는 애플리케이션이 정하고, 저장소는 그 판단에 개입하지 않는다. 아래는 저장소(`utf8mb4_unicode_ci`)가 우연히 동일시하는 축의 예시이며, 이 요구사항이 보장하는 것은 **그 동일시가 저장을 거부하는 원인이 되지 않는다**는 데까지다. 이 축들의 두 값이 최종적으로 함께 남는지는 애플리케이션의 정규화 규칙이 정하며, 이 요구사항은 그것을 약속하지 않는다.

- 히라가나 ↔ 가타카나 (`まいうぇい` / `マイウェイ`)
- 큰 가나 ↔ 작은 가나 (`ウエイ` / `ウェイ`)
- 전각 ↔ 반각 (`マイウェイ` / `ﾏｲｳｪｲ`)
- 탁점·반탁점 유무 (`ハハ` / `ババ` / `パパ`)
- 대소문자 (`Monster` / `monster`)

> 이 요구사항이 필요한 이유: 추가 정답은 "표기가 흔들려도 정답으로 인정받게" 하려고 등록하는 값이다. 저장소가 표기 흔들림을 동일시하면, 사용자가 등록하려는 바로 그 조합만 골라서 거부하게 된다. 유일성 판정은 저장소의 콜레이션이 우연히 정하는 것이 아니라 도메인이 정한다.

이 규칙은 플레이리스트의 추가 정답과 **방 생성 시 복사되는 스냅샷 양쪽에 동일하게** 적용된다. 한쪽만 적용하면 플레이리스트 저장은 성공하고 방 생성이 실패한다.

#### Scenario: 탁점만 다른 두 추가 정답이 함께 저장된다
- **WHEN** 한 트랙의 추가 정답으로 `ハハ`와 `ババ`를 함께 담아 플레이리스트 생성을 요청
- **THEN** MySQL의 `track_additional_title`에 두 행이 모두 저장되고 요청이 성공해야 한다

#### Scenario: 방 생성 시 스냅샷 복사도 같은 기준을 따른다
- **WHEN** `ハハ`와 `ババ`를 추가 정답으로 가진 플레이리스트로 방을 생성
- **THEN** MySQL의 `room_track_additional_title`에도 두 값이 모두 복사되고 방 생성이 성공해야 한다

#### Scenario: 완전히 같은 값은 하나로만 저장된다
- **WHEN** 한 트랙의 추가 정답으로 동일한 문자열을 두 번 담아 요청
- **THEN** 저장되는 행은 하나여야 한다
- **AND** 요청은 실패하지 않아야 한다

#### Scenario: 플레이리스트 수정 경로도 같은 기준을 따른다
- **WHEN** 기존 플레이리스트를 수정하면서 탁점만 다른 두 추가 정답(`ハハ`/`ババ`)을 함께 담아 요청
- **THEN** 기존 추가 정답이 삭제된 뒤 두 값이 모두 저장되고 요청이 성공해야 한다
